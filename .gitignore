node_modules
dist
.figma/diff

# build cache
tsconfig.tsbuildinfo

# Full-resolution plate originals. Large, and every shipped plate is
# regenerated from them by scripts/prepare-plate.mjs.
assets-src

# Vite dev server log - regenerated on every run.
dev.log

# Staged, id-named plate exports. public/v2 holds the named copies.
.figma/v2/stage

# Vite dev server log for the V2 port - regenerated on every run.
dev-v2.log
